/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // distDir: 'build',
  output: 'standalone',
  // eslint: {
  //   ignoreDuringBuilds: true, // ignore lint check during build
  // },
  // typescript: {
  //   ignoreBuildErrors: true, // ignore type check during build
  // },
};

export default nextConfig;
